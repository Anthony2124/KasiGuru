{
  "name": "kasiguru-functions",
  "version": "1.0.0",
  "private": true,
  "description": "Cloud Functions for KasiGuru (admin bootstrap, scheduled Firestore backups)",
  "main": "index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "deploy": "firebase deploy --only functions",
    "serve": "firebase emulators:start --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.1.0",
    "firebase-functions": "^5.0.1",
    "google-auth-library": "^9.11.0"
  }
}
